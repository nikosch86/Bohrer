# Bohrer SSH Tunnel Server Configuration
# Copy this file to .env and customize as needed

# Domain Configuration
DOMAIN=localhost
ACME_EMAIL=admin@example.com
ACME_STAGING=true

# Internal Ports (inside container) - Standard ports
SSH_PORT=22
HTTP_PORT=80
HTTPS_PORT=443

# External Ports (Docker host)
SSH_EXTERNAL_PORT=2222
HTTP_EXTERNAL_PORT=8080
HTTPS_EXTERNAL_PORT=8443

# SSH Authentication
SSH_AUTHORIZED_KEYS=/data/authorized_keys

# Production Examples (uncomment and modify):
# DOMAIN=yourdomain.com
# ACME_EMAIL=admin@yourdomain.com
# ACME_STAGING=false
# HTTP_EXTERNAL_PORT=80
# HTTPS_EXTERNAL_PORT=443